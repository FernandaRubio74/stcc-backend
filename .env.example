# --- Servidor (pendiente: se completa cuando se implemente EPIC-09) ---
PORT=3000
NODE_ENV=development

# --- Base de datos (usado por T-004: modelo de datos inicial) ---
DATABASE_URL="postgresql://ideator:ideator@localhost:5433/ideator_db?schema=public"

# --- Autenticacion (pendiente: se completa en el epic de auth/SSO) ---
JWT_SECRET=
JWT_EXPIRES_IN=1d
SSO_CLIENT_ID=
SSO_CLIENT_SECRET=
SSO_CALLBACK_URL=http://localhost:3000/auth/sso/callback

# --- IA (pendiente: se completa cuando se integre el motor conversacional) ---
AI_PROVIDER_API_KEY=
