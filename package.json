{
  "name": "stcc-backend",
  "version": "0.1.0",
  "description": "API y logica de negocio de Ideator",
  "private": true,
  "scripts": {
    "dev": "echo \"[pendiente] se implementara al definir el framework (Express/NestJS) en EPIC-09\"",
    "build": "echo \"[pendiente] se implementara al definir el framework en EPIC-09\"",
    "start": "echo \"[pendiente] se implementara al definir el framework en EPIC-09\"",
    "lint": "echo \"[pendiente] se configurara ESLint/Prettier segun convenciones del equipo\"",
    "seed": "echo \"[pendiente] sin datos de negocio definidos aun\"",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "migrate": "prisma migrate deploy --schema=src/database/schema.prisma",
    "migrate:dev": "prisma migrate dev --schema=src/database/schema.prisma",
    "generate": "prisma generate --schema=src/database/schema.prisma",
    "studio": "prisma studio --schema=src/database/schema.prisma",
    "setup": "npm run db:up && npm run migrate && npm run generate",
    "test": "jest --runInBand",
    "test:coverage": "jest --coverage --runInBand"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 20000
  }
}
